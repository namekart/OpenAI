{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "openbusiness-web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=openbusiness-web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
